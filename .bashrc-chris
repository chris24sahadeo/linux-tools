alias bashrc='source ~/.bashrc'

# Add git branch if its present to PS1
# Uncomment lines in .bashrc as specified here: https://askubuntu.com/questions/730754/how-do-i-show-the-git-branch-with-colours-in-bash-prompt
parse_git_branch() {
 git branch 2> /dev/null | sed -e '/^[^*]/d' -e 's/* \(.*\)/(\1)/'
}
if [ "$color_prompt" = yes ]; then
 PS1='${debian_chroot:+($debian_chroot)}\[\033[01;32m\]\u@\h\[\033[00m\]:\[\033[01;34m\]\w\[\033[01;31m\]$(parse_git_branch)\[\033[00m\]\$ '
else
 PS1='${debian_chroot:+($debian_chroot)}\u@\h:\w$(parse_git_branch)\$ '
fi

# Reach.
REACH_ROOT=~/project-reach
function rcd {
    # echo "$REACH_ROOT/$1"
    cd "${REACH_ROOT}/$1"
}

alias rcode='rcd && code .'
alias rgo='cd ${REACH_ROOT}/go/src/project-reach && code .'
alias rpush='git push origin HEAD:refs/for/master'

. ${REACH_ROOT}/setenv.sh

# Other aliases.
alias gcan='git commit --amend --no-edit'
alias ping='ping -c 5'